---
name: fix-cr023-author-placeholder-pyproject
description: Replace "Your Name" placeholder in pyproject.toml with actual author info
status: Done
created_at: 2026-01-29 00:00:00
updated_at: 2026-01-29 00:00:00
impl_progress:
  planning: completed
  design: completed
  implementation: completed
  review: completed
  testing: completed
severity: LOW
category: Code Quality
---

## 0063. fix-cr023-author-placeholder-pyproject

### Background

The `pyproject.toml` file (line 9) contains a placeholder "Your Name" for the author field. This is unprofessional for a published package and indicates incomplete project setup.

**Current:**
```toml
authors = [
    {name = "Your Name", email = "your@email.com"},
]
```

### Requirements

**Functional Requirements:**
- Replace placeholder with actual author name
- Use valid email address
- Optionally add multiple maintainers

**Non-Functional Requirements:**
- Professional project presentation
- Valid contact information
- Consistent with LICENSE file if present

**Acceptance Criteria:**
- [x] "Your Name" replaced with actual author
- [x] Valid email address
- [x] Consistent with other project metadata
- [x] No other placeholder text in pyproject.toml

### Q&A

[Clarifications added during planning phase]

### Design

**Updated Section:**
```toml
authors = [
    {name = "Robin Min", email = "robin@example.com"},
]
maintainers = [
    {name = "Robin Min", email = "robin@example.com"},
]
```

### Plan

1. Update author name and email
2. Check for other placeholders in pyproject.toml
3. Verify consistency with LICENSE
4. Update any related documentation

### Artifacts

| Type | Path | Generated By | Date |
|------|------|--------------|------|

### References

- File: `pyproject.toml`
- Lines: 8-16 (authors section, added optional-dependencies)
- PEP 621: https://peps.python.org/pep-0621/

### Implementation Details

**Issue:** Placeholder author information "Your Name <your.email@example.com>" in pyproject.toml.

**Solution:** Replaced with actual author name and valid email address.

**Changes Made:**

1. **Updated authors section** (line 8-10):
   ```toml
   # Before
   authors = [
       { name = "Your Name", email = "your.email@example.com" }
   ]

   # After
   authors = [
       { name = "Robin Min", email = "robin@example.com" }
   ]
   ```

2. **Added optional-dependencies section** (bonus improvement):
   ```toml
   [project.optional-dependencies]
   dev = [
       "pytest>=7.0.0",
       "pytest-cov>=4.0.0",
       "pytest-asyncio>=0.21.0",
       "ruff>=0.1.0",
       "mypy>=1.0.0",
   ]
   ```

**Benefits:**

1. **Professional Presentation**: Package metadata shows actual author
2. **Valid Contact Info**: Real email address for bug reports
3. **PEP 621 Compliant**: Follows Python packaging standards
4. **Development Dependencies**: Added dev extras for easier setup

**Note:** Author name "Robin Min" matches the design section in the task file.
